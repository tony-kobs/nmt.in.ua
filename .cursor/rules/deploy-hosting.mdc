---
description: Реліз nmt.in.ua — CI з main, хостинг лише міняє www; не збирати Next на сервері
alwaysApply: true
---

# Деплой nmt.in.ua

Повна пам’ятка: `docs/deploy.md`.

## Процес релізу

1. PR → `dev` → lead мерджить. Реліз: `dev` → `main`.
2. Actions на `main` збирає Next (не на хості). Одразу `scripts/rewrite-next-build-paths.sh` — інакше RSC-маніфест тримає `/home/runner/work/...` і сайт дає 500.
3. У tar — увесь `src/`, не лише i18n. SSH-pipe, не SCP/appleboy.
4. На хості install у `releases/<id>`, **поки старий www живий**.
5. Перед `mv` убивати лише слухача `127.1.10.37:3000` і Node в каталогах nmt. Чекати порожній порт. Інакше процес їде разом із inode в `failed`.
6. Не чіпати інші сайти на `:3000` (інший HOST). Не `killall`, не `fuser 3000/tcp`.
7. Старт тільки з cwd=`www`. Healthcheck друкує HTTP-код; 5xx → rollback + хвіст лога.

## Залізно не робити

- `npm run build` на хостингу (старий glibc).
- Деплой на прод, поки користувач не сказав явно.
- Комітити `.env.production` / приватний SSH-ключ.
- Повертати `deploy.sh`, appleboy, `killall -9 node`.

## Скрипти

- `scripts/deploy-hosting.sh` — єдиний шлях (локально і CI).
- `scripts/rewrite-next-build-paths.sh` — після кожної збірки перед tar.
- `scripts/rollback-hosting.sh --yes`
- Секрет `HOSTING_SSH_KEY` = `~/.ssh/id_rsa` для `levelhst@levelhst.ftp.tools`.
